apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mem0
  namespace: flux-system
spec:
  interval: 10m
  path: ./clusters/olympus/mem0
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: external-secrets-config
    - name: longhorn
    - name: tailscale-operator
