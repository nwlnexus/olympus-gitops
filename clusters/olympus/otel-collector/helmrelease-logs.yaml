apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: otel-logs
  namespace: otel
spec:
  interval: 1h
  chart:
    spec:
      chart: opentelemetry-collector
      version: "0.158.0"
      sourceRef:
        kind: HelmRepository
        name: open-telemetry
        namespace: otel
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    mode: daemonset
    fullnameOverride: otel-logs
    # Load-bearing: node naraka-01 (former management-hub host, now part of
    # olympus) still carries the taint olympus.io/role=management-hub:NoSchedule.
    # This toleration is what lets the log-collection DaemonSet schedule there.
    # Do NOT remove until that node is untainted/relabelled (a node-level op).
    tolerations:
      - key: olympus.io/role
        operator: Equal
        value: management-hub
        effect: NoSchedule
    image:
      repository: otel/opentelemetry-collector-contrib
    presets:
      logsCollection:
        enabled: true
        includeCollectorLogs: false
      kubernetesAttributes:
        enabled: true
    ports:
      otlp: { enabled: false }
      otlp-http: { enabled: false }
      jaeger-compact: { enabled: false }
      jaeger-thrift: { enabled: false }
      jaeger-grpc: { enabled: false }
      zipkin: { enabled: false }
      metrics: { enabled: false }
    config:
      receivers:
        jaeger: null
        prometheus: null
        zipkin: null
        otlp: null
      processors:
        memory_limiter:
          check_interval: 1s
          limit_percentage: 80
          spike_limit_percentage: 25
        batch:
          timeout: 5s
          send_batch_size: 1024
        resource:
          attributes:
            - key: k8s.cluster.name
              value: olympus
              action: upsert
      exporters:
        # olympus cluster nodes have tailscaled → MagicDNS resolves obs-loki directly.
        otlphttp/loki:
          logs_endpoint: http://obs-loki.raptor-mimosa.ts.net:3100/otlp/v1/logs
      service:
        telemetry:
          metrics:
            level: none
        pipelines:
          traces: null
          metrics: null
          logs:
            receivers: [filelog]
            processors: [memory_limiter, k8sattributes, resource, batch]
            exporters: [otlphttp/loki]
