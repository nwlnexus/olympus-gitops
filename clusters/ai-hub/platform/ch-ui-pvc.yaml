apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ch-ui-data
  namespace: platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: qnap-iscsi
  resources:
    requests:
      storage: 2Gi
